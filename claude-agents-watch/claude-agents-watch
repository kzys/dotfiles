#! /usr/bin/env python3
"""Show Claude Code sessions in a small window, refreshed from
`claude agents --json`.

Needs only python3 with tkinter. On macOS the Homebrew python has it via
`brew install python-tk`; the Linux package is usually python3-tk.
"""

import argparse
import json
import os
import queue
import subprocess
import threading
import time
import tkinter as tk
from tkinter import font as tkfont
from tkinter import ttk

# Row colours by state. Anything else falls back to the default foreground.
STATE_COLORS = {
    "working": "#1a7f37",
    "needs_input": "#b35c00",
    "failed": "#c0392b",
    "done": "#6e7781",
}


def fetch(show_all):
    """Run `claude agents --json` and return its list, or raise."""
    cmd = ["claude", "agents", "--json"]
    if show_all:
        cmd.append("--all")
    out = subprocess.run(cmd, capture_output=True, text=True, check=True)
    return json.loads(out.stdout)


def poll(show_all, interval, results, stop):
    """Fetch forever on a thread, posting (sessions, error) to results."""
    while not stop.is_set():
        try:
            results.put((fetch(show_all), None))
        except Exception as e:  # subprocess or JSON failure; show it, keep going
            results.put((None, str(e).strip() or type(e).__name__))
        stop.wait(interval)


def elapsed(started_ms):
    s = int(time.time() - started_ms / 1000)
    if s < 60:
        return f"{s}s"
    if s < 3600:
        return f"{s // 60}m"
    return f"{s // 3600}h{(s % 3600) // 60:02d}m"


def short_cwd(path):
    home = os.path.expanduser("~")
    if path == home or path.startswith(home + os.sep):
        return "~" + path[len(home):]
    return path


def sort_key(a):
    # Live sessions first, busiest first, then newest first.
    live = a.get("pid") is not None
    busy = a.get("status") == "busy"
    return (not live, not busy, -(a.get("startedAt") or 0))


class App:
    def __init__(self, root, args):
        self.root = root
        self.args = args
        self.sessions = []
        self.results = queue.Queue()
        self.stop = threading.Event()

        root.title("claude agents")
        root.attributes("-topmost", args.topmost)
        root.bind("t", self.toggle_topmost)
        root.bind("q", lambda e: self.quit())
        root.protocol("WM_DELETE_WINDOW", self.quit)

        # Packed first so the tree can't squeeze it out of a small window.
        self.status = tk.StringVar()
        ttk.Label(root, textvariable=self.status, anchor="w",
                  padding=(4, 2)).pack(side="bottom", fill="x")

        # ttk's default row height ignores the font and clips it on HiDPI.
        linespace = tkfont.nametofont("TkDefaultFont").metrics("linespace")
        ttk.Style().configure("Treeview", rowheight=linespace + 4)

        cols = ("name", "state", "elapsed", "cwd")
        self.tree = ttk.Treeview(root, columns=cols, show="headings")
        for col, title, width, stretch in (
            ("name", "Name", 220, True),
            ("state", "State", 90, False),
            ("elapsed", "Time", 60, False),
            ("cwd", "Directory", 200, True),
        ):
            self.tree.heading(col, text=title)
            self.tree.column(col, width=width, stretch=stretch,
                             anchor="e" if col == "elapsed" else "w")
        for state, color in STATE_COLORS.items():
            self.tree.tag_configure(state, foreground=color)
        self.tree.pack(fill="both", expand=True)

        threading.Thread(
            target=poll,
            args=(args.all, args.interval, self.results, self.stop),
            daemon=True,
        ).start()
        self.tick()

    def toggle_topmost(self, _event=None):
        self.root.attributes("-topmost", not self.root.attributes("-topmost"))

    def quit(self):
        self.stop.set()
        self.root.destroy()

    def tick(self):
        """Apply any poll results, then redraw so elapsed times advance."""
        try:
            while True:
                sessions, err = self.results.get_nowait()
                if err is None:
                    self.sessions = sorted(sessions, key=sort_key)
                    self.status.set(time.strftime("updated %H:%M:%S"))
                else:
                    self.status.set(f"error: {err}")
        except queue.Empty:
            pass
        self.redraw()
        self.root.after(1000, self.tick)

    def redraw(self):
        """Sync tree rows to self.sessions, keyed by session id so the
        selection survives refreshes."""
        seen = set()
        for i, a in enumerate(self.sessions):
            iid = a.get("id") or a.get("sessionId")
            seen.add(iid)
            live = a.get("pid") is not None
            state = a.get("state") or ""
            values = (
                a.get("name") or "(untitled)",
                state,
                elapsed(a["startedAt"]) if live and a.get("startedAt") else "",
                short_cwd(a.get("cwd") or ""),
            )
            if self.tree.exists(iid):
                self.tree.item(iid, values=values, tags=(state,))
                self.tree.move(iid, "", i)
            else:
                self.tree.insert("", i, iid=iid, values=values, tags=(state,))
        for iid in self.tree.get_children():
            if iid not in seen:
                self.tree.delete(iid)

        busy = sum(1 for a in self.sessions if a.get("status") == "busy")
        self.root.title(f"claude agents ({busy} busy)" if busy else "claude agents")


def main():
    p = argparse.ArgumentParser(description=__doc__.split("\n\n")[0])
    p.add_argument("--live", dest="all", action="store_false",
                   help="hide completed background sessions")
    p.add_argument("--interval", type=float, default=2.0, metavar="SEC",
                   help="seconds between refreshes (default: 2)")
    p.add_argument("--no-topmost", dest="topmost", action="store_false",
                   help="don't keep the window above others (t toggles)")
    args = p.parse_args()

    root = tk.Tk()
    root.geometry("640x240")
    App(root, args)
    root.mainloop()


if __name__ == "__main__":
    main()
